import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    header:{
        position: 'absolute',
        width: '100%',
        height: '12%',
        justifyContent:'center',
        alignItems: 'center',
    }
});